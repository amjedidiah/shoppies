{
  "name": "root",
  "private": true,
  "author": {
    "name": "Amaraegbu Jedidiah",
    "email": "imunacode@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/amjedidiah/shoppies.git"
  },
  "bugs": {
    "url": "https://github.com/amjedidiah/shoppies/issues"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist --ignore-prepublish",
    "changed": "lerna changed -a --graph --json -l",
    "list": "lerna ls -a --graph --json -l",
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream",
    "prestart": "lerna run --concurrency 1 --stream check",
    "start": "lerna run start --stream"
  },
  "pre-commit": "prestart",
  "devDependencies": {
    "lerna": "^3.22.1",
    "pre-commit": "^1.2.2"
  }
}
